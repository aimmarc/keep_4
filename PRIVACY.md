# Keep4 隐私政策

最后更新：2026 年 8 月 19 日

Keep4 是离线运行的开源 Android 应用。本政策说明应用使用权限和处理数据的方式。

## 数据处理

- Keep4 不申请联网权限，不上传数据。
- Keep4 不包含广告、分析、统计、崩溃收集或远程配置 SDK。
- 应用设置保存在设备本地的 Android DataStore 中。
- 应用不会读取联系人、位置、照片、短信、账号或设备标识符。

## 麦克风

Keep4 使用麦克风权限启动 `VOICE_COMMUNICATION` 录音路由，以触发目标设备的四扬声器行为。录音数据只写入应用私有缓存文件，不会播放、分析、共享或上传；录音停止或服务销毁时会立即删除该文件。

Android 会在麦克风使用期间显示系统隐私指示器。用户可以随时关闭 Keep4 或撤销麦克风权限。

## 无障碍服务

无障碍服务是可选能力。Keep4 不读取窗口内容，不检索界面节点，不记录输入，也不执行跨应用点击。系统重新绑定该服务时，它只检查用户是否已开启 Keep4，并尝试恢复后台服务。

## Shizuku

Shizuku 是可选能力。用户授权后，Keep4 只执行以下与自身包名有关的操作：

- 申请加入系统待机白名单；
- 将自身 `RUN_IN_BACKGROUND` AppOps 设置为允许；
- 将自身 `RUN_ANY_IN_BACKGROUND` AppOps 设置为允许。

Keep4 不通过 Shizuku 读取其他应用数据或修改其他应用设置。

## 数据删除

关闭服务会删除临时录音文件。卸载应用会由 Android 删除应用私有设置和缓存。

## 联系方式

对本政策有疑问，可通过 GitHub Issues 联系项目维护者。涉及安全或隐私漏洞时，请按 [SECURITY.md](SECURITY.md) 私下报告。
